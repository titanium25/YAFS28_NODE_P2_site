<div class="row mt-5 justify-content-md-center">
    <div class="card-body">

        <h1 class="text-center mb-3">
            Menu
        </h1>
        <div class="col-md text-center">
            <% if(permissions.vm){ %>
                <a href="/menu/movies" class="btn my-2 mx-2">Movies</a>
            <% } %>
            <% if(permissions.vs){ %>
                <a href="/menu/subs" class="btn my-2 mx-2">Subscriptions</a>
            <% } %>
            <% if(admin) { %>
                <a href="/menu/manage" class="btn my-2 mx-2">Users Management Console</a>
            <% } %>
        </div>
    </div>
</div>